{
  "include": ["src"], // directory that has files to compile
  "compilerOptions": {
    "outDir": "build",
    "target": "ES6",
    "moduleResolution": "node",
    "lib": ["ES6", "DOM"],
    "strict": true,
    "allowJs": true
  }
}
